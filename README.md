# Cell Lab (Public Reference)

This repository contains a **public, domain-specific reference slice** of Cell Lab.

- Includes: synthetic generator + small examples.
- Excludes: the domain-agnostic invariant engine and production scoring logic.

See `docs/overview.md` for boundary notes.

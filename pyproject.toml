[project]
name = "cell-lab"
version = "0.1.0"
description = "Reference artifacts for Cell Lab (public slice)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "AGPL-3.0-only" }

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
